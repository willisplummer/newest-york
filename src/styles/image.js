import styled from 'styled-components';

const Image = styled.img`
  display: block;
  margin-left: auto;
  margin-right: auto;
  width: auto;
  height: 400px;
  border-radius: 7px;
`;

export default Image;
